[package]
name = "bazbom-policy"
version = "0.2.1"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regorus = { version = "0.2", optional = true }

[features]
default = []
rego = ["regorus"]

